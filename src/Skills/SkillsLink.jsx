import React from "react";
import { Button } from 'react-bootstrap';

function SkillsLink () {
    return (
        <div className="skillse">
            <Button> Для работодателей
            (резюме)</Button>
        </div>

    );
}

export default SkillsLink;